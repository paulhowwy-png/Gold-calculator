document.getElementById('calculateBtn').addEventListener('click', function() { const weight = parseFloat(document.getElementById('weight').value); const unit = document.getElementById('unit').value; const purity = parseFloat(document.getElementById('purity').value); const pricePerGram = parseFloat(document.getElementById('pricePerGram').value); if (isNaN(weight) || isNaN(pricePerGram) || weight <= 0 || pricePerGram <= 0) { alert('Please enter valid numbers for weight and price'); return; } let weightInGrams = weight; if (unit === 'ounces') { weightInGrams = weight * 31.1035; } const pureGoldWeight = weightInGrams * (purity / 100); const totalValue = pureGoldWeight * pricePerGram; const resultDiv = document.getElementById('result'); document.getElementById('weightDisplay').textContent = weightInGrams.toFixed(2); document.getElementById('purityDisplay').textContent = purity; document.getElementById('pureGoldDisplay').textContent = pureGoldWeight.toFixed(2); document.getElementById('totalValue').textContent = totalValue.toFixed(2); resultDiv.classList.add('show'); });